mysql:
  image: muzili/mariadb:latest
  volumes:
    - /home/data/mariadb/data:/data
    - /home/data/mariadb/log:/var/log/mysql

phabricator:
  image: muzili/phabricator:latest
  links:
   - mysql:mysql
  environment:
    VIRTUAL_HOST: phab.localdomain.com
  volumes:
    - /home/data/phabricator/data:/srv/www/phabricator
    - /home/data/phabricator/git:/srv/git
    - /home/data/phabricator/log:/var/log

nginxproxy:
  image: jwilder/nginx-proxy
  ports:
    - "80:80"
    - "443:443"
  volumes:
    - /var/run/docker.sock:/tmp/docker.sock

